<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tsysv.demo.popyti"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="tsysv.demo.popyti.MainActivity"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
          <activity
            android:name="tsysv.demo.popyti.Activity02"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
         <activity
            android:name="tsysv.demo.popyti.Activity03"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
         <activity
            android:name="tsysv.demo.popyti.Main01"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Mess01"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Mess02"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Mess03"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Marshr01"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
        <activity
            android:name="tsysv.demo.popyti.Marshr02"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
          <activity
            android:name="tsysv.demo.popyti.Marshr03"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
          <activity
            android:name="tsysv.demo.popyti.Marshr04"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
          <activity
            android:name="tsysv.demo.popyti.Marshr05"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
          <activity
            android:name="tsysv.demo.popyti.Karta01"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Karta010"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Karta011"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Karta012"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Karta013"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Karta014"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Karta020"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Karta021"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Karta022"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Karta023"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
              <activity
            android:name="tsysv.demo.popyti.Profile01"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
             <activity
            android:name="tsysv.demo.popyti.Profile20"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Profile21"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Profile22"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Vopr01"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Vopr010"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Vopr011"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
            <activity
            android:name="tsysv.demo.popyti.Sloi01"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
        <activity
            android:name="tsysv.demo.popyti.Sloi02"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
        <activity
            android:name="tsysv.demo.popyti.Sloi03"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
        <activity
            android:name="tsysv.demo.popyti.Sloi04"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
         <activity
            android:name="tsysv.demo.popyti.Menu00"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
         <activity
            android:name="tsysv.demo.popyti.Menu10"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Menu11"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Menu12"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Menu13"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
           <activity
            android:name="tsysv.demo.popyti.Menu20"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
          <activity
            android:name="tsysv.demo.popyti.Menu21"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
          <activity
            android:name="tsysv.demo.popyti.Menu30"
            android:label="@string/app_name"
             android:theme="@android:style/Theme.NoTitleBar"
             >
        </activity>
        
    </application>

</manifest>
